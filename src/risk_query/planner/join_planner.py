class JoinPlanner:
    def plan(self, datasets: list[dict]) -> list[dict]:
        # Production version should solve a constrained graph path problem
        # over metadata assets and certified join edges.
        return datasets
